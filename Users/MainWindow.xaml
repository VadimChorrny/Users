<Window x:Class="Users.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Users"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="211*"/>
            <ColumnDefinition Width="582*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <Button Content="Show users" Height="50" Width="150" Click="Button_Click_1"/>
                <Button Content="New users" Height="50" Width="150" Margin="0 50 0 0" Click="Button_Click" />
            </StackPanel>
        </Grid>
        
        <Grid Grid.Column="1">
            <Frame x:Name="Main" NavigationUIVisibility="Hidden" />
        </Grid>
    </Grid>
</Window>
